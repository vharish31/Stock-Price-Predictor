{
  "name": "quantsight-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Stock price predictor dashboard with ML forecasts",
  "scripts": {
    "dev": "node server.mjs",
    "build": "vite build",
    "start": "node server.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
